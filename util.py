import json

# split the data into default 80/20 train/test
def train_test_split(data,train_ratio=0.8):
    num_samples = data.size
    num_train = train_ratio * num_samples
    num_test - num_samples - num_train

# TODO: function that returns a dict/object of N number of
def read(self,dataset,num_lines):
    print("read function")
    #while open(dataset.filename) as fin:
    []
    return data_chunk
